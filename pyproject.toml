[project]
name = "subject10-pose-action"
version = "0.1.0"
description = "Subject 10 - 2D Pose Estimation and Action Analysis"
requires-python = ">=3.11"
dependencies = [
    "ultralytics",
    "opencv-python",
    "numpy",
    "pandas",
    "scikit-learn",
    "joblib",
    "matplotlib",
    "torch",
    "tqdm",
    "fastapi>=0.141.1",
    "uvicorn>=0.53.0",
    "python-multipart>=0.0.32",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]
